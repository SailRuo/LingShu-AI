## 1. 检索反馈记录

- [ ] 1.1 在 `MemoryRetrievalEvent` DTO 中增加 `adoptedFactIds` 字段，记录被 LLM 采纳的事实 ID 列表
- [ ] 1.2 在 `retrieveContext` 方法中，从 LLM 回答或上下文组装逻辑中提取采纳的事实 ID，并填充到事件中
- [ ] 1.3 增加异步关系更新入口方法 `updateRelationshipsFromRetrievalEvent`，接收采纳事实列表并分析共现对

## 2. 图谱关系动态建立

- [ ] 2.1 实现共现对分析逻辑，统计事实对的共同采纳次数，达到阈值（2 次）时触发关系建立
- [ ] 2.2 在 `FactRepository` 中增加查询现有 `RELATED_TO` 关系权重的方法
- [ ] 2.3 实现关系新建逻辑：首次共现时创建 `RELATED_TO` 关系，初始权重 0.5
- [ ] 2.4 实现关系增强逻辑：重复共现时权重增加 0.1，上限 1.0
- [ ] 2.5 为关系新建和增强添加系统日志，便于观测

## 3. 多跳图谱检索

- [ ] 3.1 在 `performGraphRetrieval` 中增加二跳扩散逻辑：当一跳结果少于 3 个时触发
- [ ] 3.2 实现沿 `RELATED_TO` 关系的二跳查询 Cypher，按关系权重排序，最多取 5 个
- [ ] 3.3 实现二跳结果去重：过滤已在一跳结果中出现的事实
- [ ] 3.4 在 `calculateGain` 中为二跳事实的重要度贡献乘以 0.7 衰减系数

## 4. Gain 计算优化

- [ ] 4.1 在 `calculateGain` 中增加状态惩罚：`superseded` 状态事实的重要度乘以 0.3
- [ ] 4.2 在 `calculateGain` 中增加时效惩罚：`observedAt` 超过 90 天的事实重要度乘以 0.7
- [ ] 4.3 更新路由决策日志，包含多跳激活事实数量和惩罚信息

## 5. 测试与验证

- [ ] 5.1 补充单元测试覆盖共现关系建立和权重增强逻辑
- [ ] 5.2 补充单元测试覆盖二跳检索扩散和去重逻辑
- [ ] 5.3 补充单元测试覆盖 Gain 计算的时序和状态惩罚
- [ ] 5.4 运行典型记忆问答回归，确认多跳检索提升了召回覆盖率且未引入明显噪声
