## 1. 后端会话能力

- [ ] 1.1 抽取并统一当前会话解析逻辑，补充会话列表与新建会话的服务能力。
- [ ] 1.2 为聊天流式发送、同步发送、历史查询和清空历史补齐显式 `sessionId` 入参。
- [ ] 1.3 让 `TurnTimelineService`、`ChatService` 和记忆存储都严格按当前会话读写数据。
- [ ] 1.4 将 WebSocket 注册与聊天消息派发串到当前会话，避免同一用户的不同会话混用上下文。

## 2. 前端会话管理与界面

- [ ] 2.1 新增前端会话状态管理，保存 `activeSessionId`、会话列表和当前会话的本地持久化状态。
- [ ] 2.2 在对话页增加会话列表、新建对话入口和当前会话切换反馈。
- [ ] 2.3 将消息发送、历史加载、清空历史和 WebSocket 交互都绑定到当前会话。
- [ ] 2.4 在页面刷新或重连后恢复上次使用的会话，并在失效时自动回退到可用会话。

## 3. 兼容性与验证

- [ ] 3.1 补充后端测试，覆盖创建会话、切换会话、历史隔离和旧请求回退逻辑。
- [ ] 3.2 补充前端验证步骤，覆盖会话切换、新建后发送、刷新恢复和清空当前会话。
- [ ] 3.3 执行一次端到端冒烟检查，确认多会话在 HTTP 与 WebSocket 两条路径下都可用。
