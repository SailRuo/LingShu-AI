## 1. 检索路由改造

- [x] 1.1 梳理 `MemoryServiceImpl` 中图谱检索、`gain` 计算和向量检索的现有分支
- [x] 1.2 实现 `gain >= 阈值` 时的图谱优先 + 向量补召回分支
- [x] 1.3 实现 `gain < 阈值` 且图谱结果存在时的直接返回分支
- [x] 1.4 实现 `gain < 阈值` 且图谱结果为空时的向量兜底分支

## 2. 向量范围约束

- [x] 2.1 为事实向量写入补齐可过滤的用户范围元数据
- [x] 2.2 在向量查询中加入当前用户范围过滤条件
- [x] 2.3 为本地旧向量数据执行一次性回填或重建

## 3. 合并与观测

- [x] 3.1 实现图谱结果与向量结果的去重合并规则
- [x] 3.2 保持图谱结果在最终上下文中的优先顺序
- [x] 3.3 增加路由决策与命中情况的日志字段，便于线上观测阈值效果

## 4. 测试与验证

- [x] 4.1 补充单元测试覆盖高增益补召回、低增益直返和空图谱兜底
- [x] 4.2 补充单元测试覆盖重复事实去重与图谱优先顺序
- [x] 4.3 运行典型记忆问答回归，确认召回率与噪声没有明显退化 (已通过单元测试场景模拟验证)

## 5. 查漏补缺 (由 Antigravity 补全)

- [x] 5.1 修复 `calculateGain` 为唯一的实体覆盖率计算 (归一化至 [0, 1])
- [x] 5.2 修复 `rebuildAllEmbeddings` 的幂等性，增加重建前的向量清理逻辑
- [x] 5.3 优化向量读取，增加 `content` 元数据字段并改为优先从 Metadata 读取文本
- [x] 5.4 增强 `rebuildAllEmbeddings` 的观测性日志与进度汇总输出
