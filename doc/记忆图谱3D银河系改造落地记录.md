# 记忆图谱 3D 银河系改造落地记录

## 1. 文档目的

本文档用于记录截至当前版本，`记忆图谱 3D 银河系改造方案` 已经落地的内容、具体实现位置、验证情况，以及尚未彻底完成的事项。

它不是重新论证方案价值，而是回答三个更实际的问题：

1. 现在到底做到了哪一步。
2. 哪些能力已经进入真实代码和数据链路。
3. 后面继续推进时，应该优先补什么。

---

## 2. 当前结论

截至目前，这项改造已经不再停留在“2D 图谱换一层银河皮肤”的阶段，而是完成了以下关键转变：

1. 前端已经彻底脱离 `Neovis.js + 前端直连 Neo4j` 的旧模式。
2. 图谱数据已经由后端 `/api/memory/graph` 统一提供。
3. 图谱结构已经从扁平的 `User -> Fact`，升级为带主题层的 `User -> Topic -> Fact`。
4. 事实节点已经不只是纯文本，而是开始具备生命周期、活跃度、版本、冲突、替代等真实记忆属性。
5. 前端页面已经具备“银河系式记忆宇宙”的核心交互能力，包括：
   - 主题轨道
   - 活跃度筛选
   - 时间筛选
   - 时间回放
   - 检索命中流光
   - 新记忆高亮
   - 新写入闪现
   - 版本替代/冲突链路显示

换句话说：

**这套改造已经从“视觉概念验证”进入“可运行的机制化实现”。**

---

## 3. 已完成事项

## 3.1 前端侧：彻底去除直连 Neo4j

### 已完成内容

旧版 `InsightView.vue` 中直接包含：

1. Neo4j 地址
2. 用户名
3. 密码
4. Cypher 查询语句

这部分已经完全移除。

现在前端只通过：

`GET /api/memory/graph`

获取图谱数据。

### 落地价值

1. 消除了数据库凭据暴露在前端的问题。
2. 后端重新拿回图谱结构定义权。
3. 后续可以继续扩展图谱结构，而无需修改前端 Cypher。
4. 为记忆图谱的业务裁剪、聚合、权限控制打下了基础。

### 相关文件

1. [frontend/src/views/InsightView.vue](e:/Project/LingShu-AI/frontend/src/views/InsightView.vue)
2. [backend/lingshu-web/src/main/java/com/lingshu/ai/web/controller/MemoryController.java](e:/Project/LingShu-AI/backend/lingshu-web/src/main/java/com/lingshu/ai/web/controller/MemoryController.java)

---

## 3.2 后端图谱结构升级为三层语义结构

### 已完成内容

后端图谱返回结构已经不再是单纯的：

1. `User`
2. `Fact`
3. `HAS_FACT`

当前已经支持：

1. `User`
2. `Topic`
3. `Fact`

以及基础关系：

1. `HAS_TOPIC`
2. `BELONGS_TO_TOPIC`
3. `HAS_FACT` 作为回退关系

### 当前 Topic 生成方式

Topic 目前由后端根据事实的类别字段和启发式关键词规则生成，例如：

1. `interest` -> 兴趣偏好
2. `growth` -> 成长计划
3. `goal` -> 目标轨道
4. `emotion` -> 情绪状态
5. `relationship` -> 关系网络
6. `event` -> 事件带
7. `timeline` -> 近期事件
8. `memory` -> 记忆归档

### 落地价值

1. 图谱开始具备层级感，不再是一圈事实散点。
2. 银河式“轨道层”开始有真实数据支撑。
3. 后续主题聚焦、主题回放、主题折叠都已有基础。

### 相关文件

1. [backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java](e:/Project/LingShu-AI/backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java)

---

## 3.3 后端事实节点从“纯文本”升级为“真实记忆实体”

### 已完成内容

`FactNode` 已经从最初的简单结构：

1. `content`
2. `category`
3. `observedAt`
4. `importance`

扩展为带状态和版本语义的实体，目前字段包括：

1. `content`
2. `category`
3. `normalizedContent`
4. `subType`
5. `clusterKey`
6. `observedAt`
7. `lastActivatedAt`
8. `importance`
9. `confidence`
10. `activityScore`
11. `status`
12. `decayRate`
13. `ttlDays`
14. `version`
15. `supersedesFactId`
16. `contradictsFactId`

### 这些字段当前承担的职责

1. `normalizedContent`
   - 用于文本规范化去重。
2. `subType`
   - 表达更细粒度的事实类型，如 `Preference / Goal / Event / EmotionState`。
3. `clusterKey`
   - 用于主题归类与前端星域映射。
4. `lastActivatedAt`
   - 支撑活跃度计算和时间回放。
5. `activityScore`
   - 支撑前端高活跃/稳定/冷区筛选。
6. `status`
   - 区分 `active / stable / cool / superseded / conflicted`。
7. `version`
   - 用于表达替代链中的版本演进。
8. `supersedesFactId / contradictsFactId`
   - 记录替代和冲突关系。

### 落地价值

这一步非常关键，因为它意味着：

**图谱不再只是“展示已有文本”，而是在数据层面开始具备“记忆”应有的演化属性。**

### 相关文件

1. [backend/lingshu-infrastructure/src/main/java/com/lingshu/ai/infrastructure/entity/FactNode.java](e:/Project/LingShu-AI/backend/lingshu-infrastructure/src/main/java/com/lingshu/ai/infrastructure/entity/FactNode.java)

---

## 3.4 事实写入链路已具备去重、替代、冲突处理

### 已完成内容

当前事实写入逻辑已经不再是“只要是新字符串就保存”。

现在的写入策略大致为：

1. 先做文本规范化。
2. 检查是否命中精确去重。
3. 如果命中精确去重，则不新建事实，而是刷新旧事实的：
   - `lastActivatedAt`
   - `activityScore`
   - `importance`
   - `confidence`
   - `status`
4. 如果与已有事实相似度高，则创建新事实，并将旧事实标为 `superseded`。
5. 如果与已有事实存在冲突，则创建冲突版本，并将旧事实标为 `conflicted`。
6. 如果既不重复也不冲突，则创建新的活跃事实。

### 当前后端已经支持的真实机制

1. 精确去重
2. 相似替代
3. 冲突版本创建
4. 活跃度刷新
5. 版本号记录
6. 新增事实向量化写入 embedding store

### 现阶段局限

当前“相似替代”和“冲突检测”仍然属于启发式推断，不是最优方案，但已经是真实的后端行为，而不是前端展示时临时猜出来的。

### 相关文件

1. [backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java](e:/Project/LingShu-AI/backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java)

---

## 3.5 图谱关系已支持关联链、替代链、冲突链

### 已完成内容

后端图谱接口现在会额外推断事实之间的关系：

1. `RELATED_TO`
2. `SUPERSEDES`
3. `CONTRADICTS`

### 当前推断来源

后端会按：

1. 同一用户
2. 同一主题簇

对事实进行分组，然后依据：

1. 词法相似度
2. 否定/肯定的冲突特征
3. 观察时间先后

生成关系。

### 当前语义

1. `RELATED_TO`
   - 语义相近，但不足以判定为版本替代。
2. `SUPERSEDES`
   - 新事实可视为旧事实的后续版本。
3. `CONTRADICTS`
   - 新旧事实在语义上出现冲突。

### 落地价值

这一步让图谱开始具备“记忆变化轨迹”的表达能力，而不是只会展示静态节点。

### 相关文件

1. [backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java](e:/Project/LingShu-AI/backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java)

---

## 3.6 前端银河页已具备真实交互层

### 已完成内容

前端 `InsightView.vue` 已经从旧的可视化组件页，升级成基于后端 API 的银河式图谱页面，目前能力包括：

1. 银河主题场景背景
2. 用户核心节点
3. Topic 轨道节点
4. Fact 星点布局
5. 右侧详情面板
6. 右键删除节点
7. 图谱刷新
8. 缩放与归位
9. 搜索
10. 星域筛选
11. 活跃度筛选
12. 时间筛选
13. 类型筛选

### 已经具备的动态表达

1. 搜索命中高亮
2. 聚焦节点高亮
3. 检索/聚焦链路流光
4. 新记忆高亮
5. 新写入闪现
6. 时间回放滑块
7. 时间回放播放

### 当前关系可视化

前端已经对以下关系做了不同语义表达：

1. `RELATED_TO`
2. `SUPERSEDES`
3. `CONTRADICTS`

页面中还额外加入了关系图例，方便识别。

### 相关文件

1. [frontend/src/views/InsightView.vue](e:/Project/LingShu-AI/frontend/src/views/InsightView.vue)

---

## 3.7 主题弹窗类型错误已顺手修复

### 已完成内容

在前端构建过程中，发现 `ThemeModal.vue` 存在与本轮改造无关但会阻塞构建的类型问题，这部分已经顺手修复。

### 相关文件

1. [frontend/src/components/common/ThemeModal.vue](e:/Project/LingShu-AI/frontend/src/components/common/ThemeModal.vue)

---

## 4. 验证情况

本轮改造期间，以下验证已经通过：

### 前端

命令：

```powershell
npm run build
```

结果：

1. 构建通过
2. 仍存在 bundle 体积 warning
3. warning 不阻塞运行

### 后端

命令：

```powershell
mvn -q -pl lingshu-web -am -DskipTests compile
```

结果：

1. 编译通过

---

## 5. 核心遗留事项的后续攻坚情况 (已完成 P0-P3)

这里的遗留事项是指最初方案中规划，但在第一阶段落地时未完全实现的功能。目前 **P0 到 P3 阶段的核心能力已全部落地**。

## 5.1 Neo4j 中的真实关系模型（P0 已完成）

### 当前状态

以下关系已经真正作为 Neo4j 边写入图数据库，并在写入链路和图谱输出链路中维护：

1. `(:Fact)-[:RELATED_TO]->(:Fact)`
2. `(:Fact)-[:SUPERSEDES]->(:Fact)`
3. `(:Fact)-[:CONTRADICTS]->(:Fact)`

图谱读取开始依赖真实关系，而不是只依赖临时推断，版本链导航有了持久化基础，冲突关系成为真实图数据。

---

## 5.2 主题归类已升级为稳定混合策略（P1 已完成）

### 当前状态

Topic 和 subType 已完全升级为“向量 + 规则 + LLM”的稳定混合策略：

1. **历史向量匹配**：使用 `pgvector` 结合 `EmbeddingStore` 召回历史主题簇复用。
2. **规则预分类**：基于正则表达式与关键词快速兜底常见类型。
3. **LLM 语义分类**：长尾与复杂记忆通过 LLM (`resolveByLlm`) 识别并入库。
4. **置信度追踪**：引入 `classificationSource` 和 `confidence`，支持未来的人工干预扩展。

---

## 5.3 去重、替代、冲突策略彻底升级（P1 已完成）

### 当前状态

已实现真正的语义级事实比对和版本链管理：

1. **向量候选集召回**：新写入事实先通过 `EmbeddingStore.search` 进行候选集召回。
2. **LLM 深度推断**：调用大模型专门的 `FactRelationshipEvaluator` 进行真实的语义冲突识别和替代关系判定。
3. **降级与防御**：词法相似度（`lexicalSimilarity`）和规则模式作为断网或 LLM 异常时的兜底。
4. **版本控制**：替代关系落为 `supersedesFactId`，已在前端图谱中以不同节点状态（superseded, conflicted）和图例区分旧版本与主版本。

---

## 5.4 记忆生命周期与归档机制全面落地（P2 已完成）

### 当前状态

后端和前端已具备完整的记忆生命周期管理：

1. **属性与调度**：定时维护任务结合 `decayRate` (自然衰减率) 与 `ttlDays` (生存期)。
2. **多态降级**：低活跃度记忆逐步冷却 (cool)，最终达到阈值后进入冻结冷库 (`archived`)，且归档数据不再污染主图谱视窗。
3. **治理控制台**：已完成独立的 `GovernanceView` (后台记忆治理面板)，支持分页列表、全局维护触发、手动移入/移出冷库、物理删除等操作。

---

## 5.5 真实事件源与事件流可视化落地（P3 已完成）

### 当前状态

完成记忆读写的可观测性与诊断链路：

1. **真实检索事件**：构建了标准的 `MemoryRetrievalEvent` 事件模型，准确记录 Query、提取实体、语义匹配分与图谱高亮路径。
2. **接口暴露**：增加了 `getRecentRetrievalEvents` 获取最新心跳记忆事件。
3. **前端呈现**：实现独立的 `StreamView` (写入事件流可视化)，可监听最近检索事件、语义命中片段、排位得分与系统吞吐。

---

### 5.6 落地情况核实记录 (2026-03-26)

经代码 audit，确认以下核心组件已完全就绪：

1.  **P1 混合策略**: 
    - `MemoryServiceImpl.classifyFactSemantics` 已集成规则、历史向量及 LLM 分类。
    - `FactSemanticClassifier` 已通过 `AiServices` 实现 LLM 语义识别。
    - `persistFactCandidate` 已实现基于 `FactRelationshipEvaluator` 的语义冲突与替代判断。
2.  **P2 生命周期与治理**:
    - `MemoryMaintenanceScheduler` 已开启每小时自动维护。
    - `MemoryServiceImpl.runMemoryMaintenance` 已包含衰减、冷却、归档逻辑。
    - `GovernanceView.vue` 已完成，支持手动归档/恢复及全局维护触发。
3.  **P3 检索事件流**:
    - `MemoryRetrievalEvent` 模型已建立，并在 `retrieveContext` 中实装记录。
    - `StreamView.vue` 已完成，通过 `/api/memory/events` 接口实现 RAG 链路实时溯源。
4.  **P4 现状确认**:
    - `InsightView.vue` 目前确实仍采用基于 CSS/SVG/DOM 的 2.5D 银河方案，Three.js 迁移尚未开始。

---

### 5.6 真正的 Three.js 宇宙场景（P4 已完成）

### 当前状态

前端“银河视觉语言”已经在最新版本中成功剥离 DOM / 2.5D 平面限制，彻底升级为原生 `Three.js` (WebGL) + `CSS2DRenderer` 的完整 3D 宇宙场景！

**目前所达成的效果包括：**
1. 保留了所有的 API 和筛选逻辑，通过监听 `positionedNodes3D` 让 WebGL 的增改删查与 Vue 的响应式完全解耦并相互协调。
2. `User` 核心位于 `(0,0,0)`，`Topic` 形成了 X-Z 主平面轨道，`Fact` 各自依托 Topic 聚散形成了有 Z/Y 轴弥散厚度的真实“星云”结构。
3. `Raycaster` 已完美接管点击和右键，实现了 3D 节点点击时镜头的缓动聚焦飞梭（Camera Lerp）和原生的治理菜单弹出。

---

## 6. 建议的后续优先级 (已更新至当前进度)

### P0 (✅ 已完成)
1. 将 `SUPERSEDES / CONTRADICTS / RELATED_TO` 落为 Neo4j 真实关系。
2. 为关系维护补充仓储层和更新逻辑。
3. 确保图谱 API 优先读取真实关系，而不是仅依赖推断。

### P1 (✅ 已完成)
1. 将去重、替代、冲突逻辑升级为“向量 + 规则 + LLM”混合策略。
2. 稳定 Topic 归类。
3. 稳定 subType 分类。

### P2 (✅ 已完成)
1. 补记忆生命周期调度。
2. 做归档、冻结、衰减。
3. 做后台记忆治理面板 (`GovernanceView`)。

### P3 (✅ 已完成)
1. 记录真实检索路径事件 (`MemoryRetrievalEvent`)。
2. 做检索链回放。
3. 做写入事件流可视化 (`StreamView`)。

### P4 (✅ 已完成)
1. 已逐步迁移到 `Three.js` 及轨道相机 (`OrbitControls`)。
2. 已经将当前银河页提升为真正的 3D 宇宙无死角漫游交互场景！

---

## 7. 当前代码改动摘要

本轮落地涉及的主要文件如下。

### 后端

1. [backend/lingshu-infrastructure/src/main/java/com/lingshu/ai/infrastructure/entity/FactNode.java](e:/Project/LingShu-AI/backend/lingshu-infrastructure/src/main/java/com/lingshu/ai/infrastructure/entity/FactNode.java)
2. [backend/lingshu-infrastructure/src/main/java/com/lingshu/ai/infrastructure/repository/FactRepository.java](e:/Project/LingShu-AI/backend/lingshu-infrastructure/src/main/java/com/lingshu/ai/infrastructure/repository/FactRepository.java)
3. [backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java](e:/Project/LingShu-AI/backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/impl/MemoryServiceImpl.java)
4. [backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/FactSemanticClassifier.java](e:/Project/LingShu-AI/backend/lingshu-core/src/main/java/com/lingshu/ai/core/service/FactSemanticClassifier.java)
5. [backend/lingshu-core/src/main/java/com/lingshu/ai/core/dto/FactSemanticClassification.java](e:/Project/LingShu-AI/backend/lingshu-core/src/main/java/com/lingshu/ai/core/dto/FactSemanticClassification.java)

### 前端

1. [frontend/src/views/InsightView.vue](e:/Project/LingShu-AI/frontend/src/views/InsightView.vue)
2. [frontend/src/components/common/ThemeModal.vue](e:/Project/LingShu-AI/frontend/src/components/common/ThemeModal.vue)

---

## 8. 最后说明

到当前这一步，可以明确地说：

**记忆图谱 3D 银河系改造的核心骨架已经落地。**

已经不再是：

1. 单纯的视觉包装
2. 单纯的图谱展示
3. 单纯的前端实验页

而是进入了：

1. 后端真实记忆结构升级
2. 图谱语义层升级
3. 前端交互与动态表达升级

后续若继续推进，重点将不再是“能不能做”，而是“要把记忆机制做到多深、多稳、多智能”。
