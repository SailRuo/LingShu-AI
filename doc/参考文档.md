目前大模型在处理“长期记忆（Long-term Memory）”时的核心痛点，也就是过拟合（Over-fitting）与联想过度的问题。

给【灵枢】的启发
在 Java 逻辑编排层，可以设计一个**“上下文隔离开关”**：

设计思路：在调用 LLM 之前，先让模型判断当前问题与历史记忆的相关度分值。如果分值低于某个阈值，或者检测到“新建”、“全新”等关键词，就主动减少从 Neo4j 中提取的背景数据。

动态 Top-K：不要每次都固定提取 10 条记忆，而是根据当前问题的独立性，动态调整提取记忆的数量。
